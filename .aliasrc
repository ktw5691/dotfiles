#####################################
#### Aliases sourced in ~/.zshrc ####
#####################################

# Reset bluetooth connection
alias bt_reset="blueutil -p 0 && sleep 1 && blueutil -p 1"

# Mount encfs drive
alias encfsve="encfs ~/Library/Mobile\ Documents/com~apple~CloudDocs/encrypted/ ~/view-encrypted/ -- -o volname=icloud-encrypted"

# Use modern 'bat' instead of 'cat' to view file contents with formatting/syntax highlighting
alias cat=bat

# Use 'exa' instead of 'ls'
alias ls=exa
